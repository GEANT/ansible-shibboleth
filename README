= ANSIBLE CODE TO INSTALL SHIBBOLETH PRODUCTS =

1. git clone

2. Edit the [production|test|developmet].ini inventory

3. Insert "hostname.domain.name.ext.crt", "hostname.domain.name.ext.key" and "CA.crt" inside roles/apache2/files

4. Run this command to run Ansible on develoment inventory to install and configure a Web Server with Apache2 enable on SSL:

     ansible-playbook debian-webservers.yml -i development.ini

